<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="500"
             xmlns:components="clr-namespace:WheelWizard.Views.Components"
             xmlns:loc="clr-namespace:WheelWizard.Localization"
             x:Class="WheelWizard.Views.Popups.MiiManagement.MiiEditor.EditorGlasses">
    <!-- FOR EVERY EDITOR PAGE -->
    <Grid RowDefinitions="40,*">
        <!-- Updated Title -->
        <TextBlock Classes="BodyText" Margin="10" Text="{loc:T attribute.mii_section.glasses}" HorizontalAlignment="Right" />
        <Border Grid.Row="0" Background="{StaticResource BackgroundLineColor}"
                HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="1"/>
        <components:IconLabelButton Grid.Row="0" Text="{loc:T action.back}" IconData="{StaticResource ArrowLeft}"
                                    Click="BackButton_OnClick" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0"
                                    Foreground="{StaticResource TitleTextColor}" FontSize="16"
                                    HoverForeground="{DynamicResource Primary200}"/>

        <!-- END OF EVERY EDITOR PAGE -->

        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <StackPanel> <!-- Glasses Type -->
                    <components:FormFieldLabel Text="{loc:T attribute.mii.glasses_type}"/>
                    <UniformGrid Columns="5" x:Name="GlassesTypesGrid">
                        <!-- Generated elements -->
                    </UniformGrid>
                </StackPanel>
                
                <StackPanel Spacing="15" x:Name="HideIfNoGlasses">
                    <!-- Glasses Color -->
                    <StackPanel>
                        <components:FormFieldLabel Text="{loc:T attribute.mii.glasses_color}"/>
                        <UniformGrid Columns="6" x:Name="GlassesColorGrid">
                            <!-- Generated elements -->
                        </UniformGrid>
                    </StackPanel>

                    <!-- Vertical Position (Up/Down) -->
                    <StackPanel>
                        <components:FormFieldLabel Text="{loc:T attribute.mii.vertical_position}"/>
                        <Border Background="{DynamicResource Neutral950}" CornerRadius="6" Padding="2" HorizontalAlignment="Left">
                            <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                                <components:Button x:Name="VerticalDecreaseButton" IconData="{StaticResource ArrowUp}" IconSize="14" Padding="4" Click="VerticalDecrease_Click"/>
                                <TextBlock x:Name="VerticalValueText" Classes="BodyText" MinWidth="30" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center"/>
                                <components:Button x:Name="VerticalIncreaseButton" IconData="{StaticResource  ArrowDown}" IconSize="14" Padding="4"  Click="VerticalIncrease_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Size (Zoom In/Out) -->
                    <StackPanel>
                        <components:FormFieldLabel Text="{loc:T attribute.mii.size}"/>
                        <Border Background="{DynamicResource Neutral950}" CornerRadius="6" Padding="2" HorizontalAlignment="Left">
                            <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                                <components:Button x:Name="SizeDecreaseButton" IconData="{StaticResource MinMark}" IconSize="14" Padding="4" Click="SizeDecrease_Click"/>
                                <TextBlock x:Name="SizeValueText" Classes="BodyText" MinWidth="30" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center"/>
                                <components:Button x:Name="SizeIncreaseButton" IconData="{StaticResource  PlusMark}" IconSize="14" Padding="4"  Click="SizeIncrease_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

    </Grid>
</UserControl>